export const NSLicenseResult = `/** 
Carrier object for LicenseResult.
*/
class NSLicenseResult {
    String GetReason();
    Bool GetSucceeded();
    Void SetReason(String reason);
    Void SetSucceeded(Bool succeeded);
}`;